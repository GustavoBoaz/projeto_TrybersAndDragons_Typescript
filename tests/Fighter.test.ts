describe('Fighter', () => {
  describe('6 - Interface Fighter', () => {
    it('A interface Fighter existe', () => {
      expect('Fighter.exists').toCompile();
    });
    it('A interface fighter possui o atributo', () => {
      expect('Fighter.').toCompile();
    });
  });
});
